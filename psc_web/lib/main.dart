import 'package:flutter/material.dart';
// import 'package:flutter/rendering.dart';
import 'package:psc_web/app.dart'; // 确保路径正确

void main() {
  // // 开启硬件加速
  // RenderingFlutterBinding.ensureInitialized();
  // // 启用图像缓存
  // imageCache.maximumSize = 500;
  runApp(const App());
}
